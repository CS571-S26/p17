# Group 17 Web Project

initial vite react typescript app

clone this repo and run `npm i`

to run locally, use `npm run dev`

to build to `/docs`, use `npm run build`

to update github pages deployment, build before committing and pushing changes, github pages will update automatically with whatever is in `/docs`